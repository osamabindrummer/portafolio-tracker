[project]
name = "portafolio-tracker"
version = "0.1.0"
description = "Backend y utilidades para Portafolio Tracker"
requires-python = ">=3.10,<3.15"
dependencies = [
  "numpy>=1.24",
  "pandas>=1.5",
  "vercel>=0.5.2",
  "yfinance>=0.2.40",
]
